name = "mirascope-website"
compatibility_date = "2024-04-07"

# Pages build configuration
pages_build_output_dir = "dist"

# Build command with clean bun install 
[build]
command = "rm -rf node_modules bun.lock && bun install && bun run build"