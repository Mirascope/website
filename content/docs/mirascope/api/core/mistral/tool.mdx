---
# AUTO-GENERATED API DOCUMENTATION - DO NOT EDIT
title: mirascope.core.mistral.tool
description: API documentation for mirascope.core.mistral.tool
---

# mirascope.core.mistral.tool

<ApiType type="Module" />

The `MistralTool` class for easy tool usage with Mistral LLM calls.

<Info title="Usage">

[Tools](/docs/mirascope/learn/tools)

</Info>

## Classes

### MistralTool

<ApiType type="Class" />

A class for defining tools for Mistral LLM calls.

Example:

```python
from mirascope.core import prompt_template
from mirascope.core.mistral import mistral_call


def format_book(title: str, author: str) -> str:
    return f"{title} by {author}"


@mistral_call("mistral-large-latest", tools=[format_book])
def recommend_book(genre: str) -> str:
    return f"Recommend a {genre} book"

response = recommend_book("fantasy")
if tool := response.tool:  # returns a `MistralTool` instance
    print(tool.call())
```

**Bases:** BaseTool

<AttributesTable
  attributes={[
  {
    "name": "tool_call",
    "type": "SkipJsonSchema[ToolCall]"
  }
]}
/>



