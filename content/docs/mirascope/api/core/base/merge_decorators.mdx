---
# AUTO-GENERATED API DOCUMENTATION - DO NOT EDIT
title: mirascope.core.base.merge_decorators
description: API documentation for mirascope.core.base.merge_decorators
---

# mirascope.core.base.merge_decorators

<ApiType type="Module" />

## Functions

### merge_decorators

<ApiType type="Function" />

## Description

Combines multiple decorators into a single decorator factory.

This function allows you to merge multiple decorators into a single decorator factory.
The decorators are applied in the order they are passed to the function.
All function metadata (e.g. docstrings, function name) is preserved through the decoration chain.

Args:
    decorator: The base decorator that determines the type signature of the decorated function.
    *additional_decorators: Additional decorators to be merged with the base decorator.

Returns:
    A decorator factory function that applies all decorators in the specified order.

Example:
    ```python
    # Combine multiple decorators
    merged = merge_decorators(
        retry(),
        with_logging(),
        handle_errors()
    )

    # Apply the merged decorators
    @merged()
    def my_function():
        pass
    ```

<ParametersTable
  parameters={[
  {
    "name": "decorator",
    "type": "Callable[[Callable[_P, _R]], Callable[_WP, _WR]]",
    "module_context": "collections.abc",
    "is_builtin": "false"
  },
  {
    "name": "additional_decorators",
    "type": "Callable[[Callable], Callable]",
    "module_context": "collections.abc",
    "is_builtin": "false",
    "default": "()"
  }
]}
  contentSubpath="docs/mirascope"
  currentModule="mirascope.core.base.merge_decorators"
/>

<ReturnType
  type="Callable[[], Callable[[Callable[_P, _R]], Callable[_WP, _WR]]]"
  moduleContext="collections.abc"
  isBuiltin={false}
  contentSubpath="docs/mirascope"
  currentModule="mirascope.core.base.merge_decorators"
  description="A decorator factory function that applies all decorators in the specified order."
/>



